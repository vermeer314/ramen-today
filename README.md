src/
├── api/ # Supabase와 통신하는 함수들 (fetch, insert 등)
├── components/ # 공통 UI 컴포넌트 (버튼, 카드, 모달 등)
├── hooks/ # 데이터 로딩 상태 등을 관리하는 커스텀 훅
├── lib/ # supabase.ts
├── pages/ # 실제 화면 (AdminPage, HomePage 등)
├── types/ # TypeScript 타입 정의 (Shops, Events 등)
├── App.tsx # 메인 라우팅
└── main.tsx # 진입점
